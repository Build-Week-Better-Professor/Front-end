import React from 'react';

const initialState = {
data: {},

isFetching: false,
 error: '',
isPosting: false
}

const reducer = (state=initialState, action) => {
switch(action.type) {
 default:
 return state
}
}
export default reducer;
